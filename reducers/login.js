const initialState = {
	email: ""
	password: ""
	confirm_password: ""
};

export default function login(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
